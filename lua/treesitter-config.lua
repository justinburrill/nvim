-- TODO: finish moving
Config = {
    sync_install = true,
    ignore_install = {},
    auto_install = true,
    highlight = {
        enable = true,
    },
    modules = {},
    rainbow = {
        enable = true,
        extended_mode = true,
        max_file_lines = 10000,
        -- colors = {},
    },
}
return Config
